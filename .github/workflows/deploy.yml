steps:
  - uses: actions/checkout@v4  # Properly clones your repo

  - name: Debug - List files
    run: ls -la  # Verify files are cloned correctly

  - name: Install dependencies
    run: npm install
    working-directory: ./  # Ensures it runs in root
